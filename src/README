* javac *.java
* java BeFinanced

********************
if you have issues with either .ser file run
* java User
* java Account

each has a main method that rewrites their respective .ser file with dummy data.
Obviously these methods will need to be removed if actually deployed.
